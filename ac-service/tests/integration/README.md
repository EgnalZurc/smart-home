# AC Service Integration Tests

These tests exercise AC controller business logic.

Original tests were in `backend/tests/integration/test_f0_*.py`.
They can be moved here and run with `pythonpath = src/` pointing to ac-service/src/.
